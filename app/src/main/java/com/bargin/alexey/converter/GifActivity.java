package com.bargin.alexey.converter;import android.os.Bundle;import android.support.v7.app.AppCompatActivity;import android.widget.Toast;import java.io.IOException;import pl.droidsonroids.gif.GifDrawable;import pl.droidsonroids.gif.GifImageView;/** * Created by Denys Parvadov on 02.02.17. */public class GifActivity extends AppCompatActivity {    public static String gif_path;    public static String gif_name;    private GifImageView mWebView;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_gif);        mWebView = (GifImageView) findViewById(R.id.gif);//        File gif = new File(gif_path, );        String path = gif_path + gif_name;        GifDrawable gifFromPath = null;        try {            gifFromPath = new GifDrawable(path);        } catch (IOException e) {            e.printStackTrace();        }        Toast.makeText(this, path, Toast.LENGTH_LONG).show();        mWebView.setBackground(gifFromPath);//        mWebView.getPaddingLeft().//        mWebView.getSettings().setJavaScriptEnabled(true);//        mWebView.loadUrl(path);    }}